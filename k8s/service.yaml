apiVersion: v1
kind: Service
metadata:
  name: invoiceflow
  namespace: default
spec:
  selector:
    app: invoiceflow
    slot: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer