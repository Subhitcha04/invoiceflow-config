apiVersion: v1
kind: Service
metadata:
  name: invoiceflow-green
  namespace: invoiceflow
spec:
  selector:
    app: invoiceflow
    slot: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP